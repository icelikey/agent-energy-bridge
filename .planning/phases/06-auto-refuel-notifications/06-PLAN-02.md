---
plan: "06-PLAN-02"
phase: 6
wave: 2
title: "AutoRefuelDecorator 接入 NotificationService + quietHours"
depends_on: ["06-PLAN-01"]
requirements: [FUEL-02, FUEL-04, FUEL-05, NOTF-01, NOTF-02]
files_modified:
  - src/adapters/auto-refuel-decorator.js
autonomous: true
---

# Plan 02: AutoRefuelDecorator 接入 NotificationService

## Objective

在 `AutoRefuelDecorator` 中注入 `NotificationService`，在 `_logAlert` 触发时按事件类型路由到控制台（NOTF-01）和多渠道通知（NOTF-02~04），并支持 `quietHours` 免打扰配置（FUEL-05）。

## must_haves

```yaml
truths:
  - notificationService 通过构造函数 options 注入，不强制要求（null 时静默跳过）
  - _logAlert 中通知发送必须 fire-and-forget（.catch(() => {})），不能阻塞 getBalance()
  - NOTF-01：refuel_failed/balance_exhausted → console.error；其余 → console.warn
  - FUEL-05：quietHours 配置 { start: number, end: number }，在免打扰时段内跳过通知
  - 事件类型到通知 level 的映射：refuel_failed/balance_exhausted → critical；refuel_success → info；其余 → warn
  - notifyTargets 可选，传入时覆盖 sendFromEnv 的默认 env 目标
```

## Tasks

### Task 1: 扩展 AutoRefuelDecorator 构造函数

<read_first>
- src/adapters/auto-refuel-decorator.js — 现有构造函数，_logAlert 方法，全文
- src/utils/quiet-hours.js — isInQuietHours 签名（PLAN-01 创建）
</read_first>

<action>
在 `src/adapters/auto-refuel-decorator.js` 中：

1. 在文件顶部 `const { GatewayAdapter } = require('./gateway-adapter');` 之后添加：
```js
const { isInQuietHours } = require('../utils/quiet-hours');
```

2. 在构造函数中，`this.onAlert = options.onAlert || null;` 之后添加：
```js
    this.notificationService = options.notificationService || null;
    this.notifyTargets = options.notifyTargets || null;
    this.quietHours = options.quietHours || null;
```

3. 将现有 `_logAlert(type, meta)` 方法替换为：
```js
  _logAlert(type, meta) {
    const alert = { type, timestamp: new Date().toISOString(), ...meta };
    this._alertLog.push(alert);
    if (this._alertLog.length > this._maxAlertLog) {
      this._alertLog = this._alertLog.slice(-this._maxAlertLog);
    }
    if (typeof this.onAlert === 'function') {
      this.onAlert(alert);
    }

    // NOTF-01: 控制台告警
    const isCritical = type === 'refuel_failed' || (meta && meta.availableUsd === 0);
    if (isCritical) {
      console.error('[AEB CRITICAL]', type, meta);
    } else {
      console.warn('[AEB WARN]', type, meta);
    }

    // NOTF-02~04: 多渠道通知（fire-and-forget，不阻塞主流程）
    if (this.notificationService && !isInQuietHours(this.quietHours)) {
      this._emitNotification(type, meta).catch(() => {});
    }
  }

  async _emitNotification(type, meta) {
    const levelMap = {
      refuel_failed: 'critical',
      balance_exhausted: 'critical',
      refuel_success: 'info',
    };
    const level = levelMap[type] || 'warn';

    const titleMap = {
      refuel_failed: 'Auto-refuel failed',
      refuel_success: 'Auto-refuel succeeded',
      balance_exhausted: 'Balance exhausted',
      refuel_cooldown: 'Refuel on cooldown',
      refuel_limit_exceeded: 'Refuel limit reached',
      refuel_no_method: 'No refuel method available',
    };
    const title = titleMap[type] || type;
    const message = meta
      ? Object.entries(meta).map(([k, v]) => `${k}: ${v}`).join(' | ')
      : '';

    const notification = { type, level, title, message, meta };

    if (this.notifyTargets) {
      return this.notificationService.send({ ...notification, targets: this.notifyTargets });
    }
    return this.notificationService.sendFromEnv(notification);
  }
```
</action>

<acceptance_criteria>
- `src/adapters/auto-refuel-decorator.js` 顶部 require `'../utils/quiet-hours'`
- 构造函数包含 `this.notificationService`、`this.notifyTargets`、`this.quietHours`
- `_logAlert` 包含 `console.error` 和 `console.warn` 分支
- `_logAlert` 包含 `isInQuietHours(this.quietHours)` 检查
- `_emitNotification` 方法存在，包含 levelMap 和 titleMap
- 通知调用为 `.catch(() => {})` fire-and-forget 模式
</acceptance_criteria>

## Verification

```bash
node -e "
const { AutoRefuelDecorator } = require('./src/adapters/auto-refuel-decorator');
const adapter = { getBalance: async () => ({ availableUsd: 10 }), listModels: async () => [], getUsage: async () => ({}), redeemCode: async () => ({}), issueKey: async () => ({}), rotateKey: async () => ({}), renderDocs: async () => '' };
const dec = new AutoRefuelDecorator(adapter, { notificationService: null, quietHours: { start: 0, end: 0 } });
console.log('notificationService:', dec.notificationService);
console.log('quietHours:', dec.quietHours);
console.log('_emitNotification exists:', typeof dec._emitNotification === 'function');
"
```
