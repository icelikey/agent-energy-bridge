---
plan: "06-PLAN-01"
phase: 6
wave: 1
title: "NotificationService 扩展：wecom + email 渠道"
depends_on: []
requirements: [NOTF-03, NOTF-04, NOTF-05]
files_modified:
  - src/service/notification-service.js
  - src/utils/quiet-hours.js
autonomous: true
---

# Plan 01: NotificationService 扩展 + quiet-hours 工具

## Objective

在现有 `NotificationService` 中新增企业微信（wecom）和邮件（email via HTTP API）两个渠道，并创建 `quiet-hours.js` 工具函数，为 PLAN-02 的 AutoRefuelDecorator 扩展提供基础。

## must_haves

```yaml
truths:
  - wecom channel 使用 msgtype:markdown + markdown.content 格式（企业微信 Webhook API）
  - email channel 通过 HTTP API 发送（AEB_NOTIFY_EMAIL_API_URL + AEB_NOTIFY_EMAIL_API_KEY），不实现 SMTP
  - _parseEnvTargets() 新增解析 AEB_NOTIFY_WECOM_URL 和 AEB_NOTIFY_EMAIL_URL
  - quiet-hours.js 正确处理跨午夜区间（start > end 时用 OR 逻辑）
  - 所有新渠道在 env var 未配置时静默跳过（graceful degradation）
  - 不引入任何 npm 依赖，全部使用 Node.js 内置模块
```

## Tasks

### Task 1: 创建 src/utils/quiet-hours.js

<read_first>
- src/utils/logger.js — 了解 utils 目录现有文件风格
</read_first>

<action>
创建文件 `src/utils/quiet-hours.js`：

```js
function isInQuietHours(quietHours) {
  if (!quietHours) return false;
  const { start, end } = quietHours;
  if (typeof start !== 'number' || typeof end !== 'number') return false;
  const hour = new Date().getHours();
  // 跨午夜区间（如 22:00-08:00）用 OR 逻辑
  if (start > end) return hour >= start || hour < end;
  return hour >= start && hour < end;
}

module.exports = { isInQuietHours };
```
</action>

<acceptance_criteria>
- `src/utils/quiet-hours.js` 文件存在
- `isInQuietHours(null)` 返回 false
- `isInQuietHours({ start: 22, end: 8 })` 在 23:00 返回 true（跨午夜）
- `isInQuietHours({ start: 9, end: 18 })` 在 10:00 返回 true
</acceptance_criteria>

### Task 2: 在 NotificationService 中新增 wecom 渠道

<read_first>
- src/service/notification-service.js — 现有 _dingtalkSender 模式，registerChannel 调用位置
</read_first>

<action>
在 `src/service/notification-service.js` 中：

1. 在构造函数 `this.registerChannel('slack', ...)` 之后添加：
```js
    this.registerChannel('wecom', this._wecomSender.bind(this));
    this.registerChannel('email', this._emailSender.bind(this));
```

2. 在 `_slackSender` 方法之后、`_hashNotification` 之前添加两个新方法：

```js
  async _wecomSender(target, payload) {
    const body = {
      msgtype: 'markdown',
      markdown: {
        content: `### ${payload.title}\n${payload.message}\n> Event: ${payload.type} | Level: ${payload.level}`,
      },
    };
    return this._webhookSender({ ...target, config: body }, payload);
  }

  async _emailSender(target, payload) {
    // HTTP API email (Mailgun/SendGrid/Aliyun DM compatible)
    const apiUrl = target.url || process.env.AEB_NOTIFY_EMAIL_API_URL;
    const apiKey = target.apiKey || process.env.AEB_NOTIFY_EMAIL_API_KEY;
    const to = target.to || process.env.AEB_NOTIFY_EMAIL_TO;
    const from = target.from || process.env.AEB_NOTIFY_EMAIL_FROM || 'aeb@noreply.local';

    if (!apiUrl || !to) {
      throw new Error('email channel requires url and to address');
    }

    const emailPayload = {
      from,
      to,
      subject: `[AEB ${payload.level.toUpperCase()}] ${payload.title}`,
      text: `${payload.message}\n\nEvent: ${payload.type}\nTimestamp: ${new Date().toISOString()}`,
    };

    const headers = {};
    if (apiKey) headers['authorization'] = `Bearer ${apiKey}`;

    return this._webhookSender({ url: apiUrl, headers }, emailPayload);
  }
```

3. 在 `_parseEnvTargets()` 方法中，在 `slackUrl` 块之后添加：
```js
    const wecomUrl = process.env.AEB_NOTIFY_WECOM_URL;
    if (wecomUrl) {
      targets.push({ channel: 'wecom', url: wecomUrl });
    }

    const emailApiUrl = process.env.AEB_NOTIFY_EMAIL_API_URL;
    const emailTo = process.env.AEB_NOTIFY_EMAIL_TO;
    if (emailApiUrl && emailTo) {
      targets.push({ channel: 'email', url: emailApiUrl, to: emailTo });
    }
```
</action>

<acceptance_criteria>
- `NotificationService` 构造函数中 `this.channels` 包含 'wecom' 和 'email'
- `_wecomSender` 方法存在，body 包含 `msgtype: 'markdown'`
- `_emailSender` 方法存在，在缺少 url/to 时抛出错误
- `_parseEnvTargets()` 解析 `AEB_NOTIFY_WECOM_URL` 和 `AEB_NOTIFY_EMAIL_API_URL`
- `node -e "const {NotificationService}=require('./src/service/notification-service'); const s=new NotificationService(); console.log([...s.channels.keys()])"` 输出包含 wecom 和 email
</acceptance_criteria>

## Verification

```bash
node -e "
const { NotificationService } = require('./src/service/notification-service');
const s = new NotificationService();
console.log('channels:', [...s.channels.keys()]);
console.log('wecom registered:', s.channels.has('wecom'));
console.log('email registered:', s.channels.has('email'));
"
node -e "
const { isInQuietHours } = require('./src/utils/quiet-hours');
console.log('null:', isInQuietHours(null));
console.log('no quietHours:', isInQuietHours(undefined));
"
```
