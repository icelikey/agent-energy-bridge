---
plan: "06-PLAN-04"
phase: 6
wave: 3
title: "HTTP 端点 + 测试覆盖"
depends_on: ["06-PLAN-02", "06-PLAN-03"]
requirements: [FUEL-01, FUEL-02, FUEL-03, FUEL-04, FUEL-05, NOTF-01, NOTF-02, NOTF-03, NOTF-04, NOTF-05]
files_modified:
  - src/server/handlers/notify.js
  - src/server/handlers/refuel-status.js
  - src/server/router.js
  - test/auto-refuel-decorator.test.js
  - test/notification-service.test.js
  - test/refuel-orchestrator.test.js
autonomous: true
---

# Plan 04: HTTP 端点 + 测试覆盖

## Objective

新增 3 个 HTTP 端点（`/agent/v1/notify/config`、`/agent/v1/notify/test`、`/agent/v1/refuel/status`），并为 Phase 6 所有新功能补充测试，确保全套测试通过。

## must_haves

```yaml
truths:
  - notify.js 和 refuel-status.js 遵循 meter.js handler 模式（requireService + sendJson）
  - 路由注册到 src/server/router.js，遵循现有 ROUTES 数组格式
  - 测试使用 node:test 内置框架，无外部依赖
  - 新增测试不破坏现有 203 个测试
  - NOTF-05 去重验证：同一事件 5 分钟内第二次 send() 返回 { sent: false, reason: 'deduplicated' }
```

## Tasks

### Task 1: 创建 src/server/handlers/notify.js

<read_first>
- src/server/handlers/meter.js — handler 模式：requireService、sendJson、validateBody
- src/server/middleware/json-body.js — readJsonBody 签名
- src/server/middleware/error-handler.js — sendError 签名
</read_first>

<action>
创建文件 `src/server/handlers/notify.js`：

```js
const { readJsonBody } = require('../middleware/json-body');
const { sendJson } = require('../middleware/send-json');
const { sendError } = require('../middleware/error-handler');

function requireNotificationService(context) {
  if (!context.notificationService) {
    const error = new Error('Notification service not available');
    error.statusCode = 503;
    error.code = 'SERVICE_NOT_CONFIGURED';
    throw error;
  }
}

async function getNotifyConfig(request, response, context) {
  requireNotificationService(context);
  const channels = [...context.notificationService.channels.keys()];
  const configured = [];

  if (process.env.AEB_NOTIFY_WEBHOOK_URL) configured.push('webhook');
  if (process.env.AEB_NOTIFY_FEISHU_URL) configured.push('feishu');
  if (process.env.AEB_NOTIFY_DINGTALK_URL) configured.push('dingtalk');
  if (process.env.AEB_NOTIFY_SLACK_URL) configured.push('slack');
  if (process.env.AEB_NOTIFY_WECOM_URL) configured.push('wecom');
  if (process.env.AEB_NOTIFY_EMAIL_API_URL && process.env.AEB_NOTIFY_EMAIL_TO) configured.push('email');

  sendJson(response, {
    availableChannels: channels,
    configuredChannels: configured,
    dedupWindowMs: context.notificationService.opts.dedupWindowMs,
  });
}

async function postNotifyTest(request, response, context) {
  requireNotificationService(context);
  const body = await readJsonBody(request, { maxBodySize: 4096 });
  const channel = body.channel || null;
  const level = body.level || 'info';
  const title = body.title || 'AEB Test Notification';
  const message = body.message || 'This is a test notification from Agent Energy Bridge.';

  const notification = {
    type: 'test',
    level,
    title,
    message,
    meta: { source: 'manual_test', timestamp: new Date().toISOString() },
  };

  let result;
  if (channel) {
    const url = body.url;
    if (!url) {
      const err = new Error('url is required when channel is specified');
      err.statusCode = 400;
      err.code = 'MISSING_URL';
      throw err;
    }
    result = await context.notificationService.send({
      ...notification,
      targets: [{ channel, url }],
    });
  } else {
    result = await context.notificationService.sendFromEnv(notification);
  }

  sendJson(response, result);
}

module.exports = { getNotifyConfig, postNotifyTest };
```
</action>

<acceptance_criteria>
- `src/server/handlers/notify.js` 文件存在
- 包含 `getNotifyConfig` 和 `postNotifyTest` 函数
- `requireNotificationService` 在 context.notificationService 为 null 时抛出 503
</acceptance_criteria>

### Task 2: 创建 src/server/handlers/refuel-status.js

<read_first>
- src/server/handlers/meter.js — handler 模式参考
</read_first>

<action>
创建文件 `src/server/handlers/refuel-status.js`：

```js
const { sendJson } = require('../middleware/send-json');

function requireAutoRefuel(context) {
  if (!context.adapter || typeof context.adapter.getAlertLog !== 'function') {
    const error = new Error('Auto-refuel decorator not available');
    error.statusCode = 503;
    error.code = 'SERVICE_NOT_CONFIGURED';
    throw error;
  }
}

async function getRefuelStatus(request, response, context) {
  requireAutoRefuel(context);
  const url = new URL(request.url, 'http://localhost');
  const limit = Math.min(Number(url.searchParams.get('limit') || 50), 500);

  const stats = context.adapter.getRefuelStats();
  const alertLog = context.adapter.getAlertLog(limit);

  sendJson(response, {
    stats,
    alertLog,
    degraded: context._degraded || false,
  });
}

module.exports = { getRefuelStatus };
```
</action>

<acceptance_criteria>
- `src/server/handlers/refuel-status.js` 文件存在
- 包含 `getRefuelStatus` 函数
- `requireAutoRefuel` 检查 `context.adapter.getAlertLog` 是否为函数
</acceptance_criteria>

### Task 3: 注册新路由到 router.js

<read_first>
- src/server/router.js — ROUTES 数组，现有 import 列表
</read_first>

<action>
在 `src/server/router.js` 中：

1. 在现有 handler imports 之后添加：
```js
const { getNotifyConfig, postNotifyTest } = require('./handlers/notify');
const { getRefuelStatus } = require('./handlers/refuel-status');
```

2. 在 ROUTES 数组中（在 routing 路由之后）添加：
```js
  { method: 'GET',  path: '/agent/v1/notify/config',  handler: getNotifyConfig },
  { method: 'POST', path: '/agent/v1/notify/test',    handler: postNotifyTest },
  { method: 'GET',  path: '/agent/v1/refuel/status',  handler: getRefuelStatus },
```
</action>

<acceptance_criteria>
- `src/server/router.js` 包含 `require('./handlers/notify')`
- `src/server/router.js` 包含 `require('./handlers/refuel-status')`
- ROUTES 数组包含 `/agent/v1/notify/config`、`/agent/v1/notify/test`、`/agent/v1/refuel/status`
</acceptance_criteria>

### Task 4: 补充 test/auto-refuel-decorator.test.js 测试

<read_first>
- test/auto-refuel-decorator.test.js — 现有 18 个测试，makeAdapter helper 模式
</read_first>

<action>
在 `test/auto-refuel-decorator.test.js` 末尾追加以下测试：

```js
// ----------------------------------------------------------------
// Phase 6: NotificationService 集成 (FUEL-02, FUEL-04, FUEL-05, NOTF-01)
// ----------------------------------------------------------------

test('_logAlert calls notificationService.sendFromEnv on low balance event', async () => {
  const sent = [];
  const mockNotify = {
    sendFromEnv: async (n) => { sent.push(n); return { sent: true }; },
    send: async (n) => { sent.push(n); return { sent: true }; },
  };
  const adapter = {
    getBalance: async () => ({ availableUsd: 1 }),
    listModels: async () => [],
    getUsage: async () => ({}),
    redeemCode: async () => ({}),
    issueKey: async () => ({}),
    rotateKey: async () => ({}),
    renderDocs: async () => '',
  };
  const dec = new AutoRefuelDecorator(adapter, {
    notificationService: mockNotify,
    lowBalanceThresholdUsd: 5,
    autoRefuelEnabled: false,
  });
  dec._logAlert('refuel_cooldown', { availableUsd: 1 });
  await new Promise((r) => setTimeout(r, 20));
  assert.ok(sent.length > 0, 'notification should have been sent');
  assert.equal(sent[0].type, 'refuel_cooldown');
});

test('_logAlert skips notification during quiet hours', async () => {
  const sent = [];
  const mockNotify = {
    sendFromEnv: async (n) => { sent.push(n); return { sent: true }; },
  };
  const adapter = {
    getBalance: async () => ({ availableUsd: 10 }),
    listModels: async () => [],
    getUsage: async () => ({}),
    redeemCode: async () => ({}),
    issueKey: async () => ({}),
    rotateKey: async () => ({}),
    renderDocs: async () => '',
  };
  // quietHours covers all 24 hours (start === end edge case: use 0-24 workaround via start=0,end=0 → not quiet)
  // Use start=0, end=24 is invalid; instead force by using a range that always matches current hour
  const currentHour = new Date().getHours();
  const dec = new AutoRefuelDecorator(adapter, {
    notificationService: mockNotify,
    quietHours: { start: currentHour, end: (currentHour + 1) % 24 },
    autoRefuelEnabled: false,
  });
  dec._logAlert('refuel_cooldown', { availableUsd: 1 });
  await new Promise((r) => setTimeout(r, 20));
  assert.equal(sent.length, 0, 'notification should be suppressed during quiet hours');
});

test('_logAlert uses console.error for critical events', () => {
  const errors = [];
  const origError = console.error;
  console.error = (...args) => errors.push(args);
  const adapter = {
    getBalance: async () => ({ availableUsd: 10 }),
    listModels: async () => [],
    getUsage: async () => ({}),
    redeemCode: async () => ({}),
    issueKey: async () => ({}),
    rotateKey: async () => ({}),
    renderDocs: async () => '',
  };
  const dec = new AutoRefuelDecorator(adapter, { autoRefuelEnabled: false });
  dec._logAlert('refuel_failed', { availableUsd: 0 });
  console.error = origError;
  assert.ok(errors.length > 0, 'console.error should be called for refuel_failed');
});

test('_logAlert uses console.warn for non-critical events', () => {
  const warns = [];
  const origWarn = console.warn;
  console.warn = (...args) => warns.push(args);
  const adapter = {
    getBalance: async () => ({ availableUsd: 10 }),
    listModels: async () => [],
    getUsage: async () => ({}),
    redeemCode: async () => ({}),
    issueKey: async () => ({}),
    rotateKey: async () => ({}),
    renderDocs: async () => '',
  };
  const dec = new AutoRefuelDecorator(adapter, { autoRefuelEnabled: false });
  dec._logAlert('refuel_cooldown', { availableUsd: 3 });
  console.warn = origWarn;
  assert.ok(warns.length > 0, 'console.warn should be called for refuel_cooldown');
});
```
</action>

<acceptance_criteria>
- `test/auto-refuel-decorator.test.js` 新增 4 个测试
- 测试覆盖 notificationService 集成、quietHours 抑制、console.error/warn 分支
</acceptance_criteria>

### Task 5: 补充 test/notification-service.test.js 测试

<read_first>
- test/notification-service.test.js — 现有 8 个测试，mock sender 模式
</read_first>

<action>
在 `test/notification-service.test.js` 末尾追加以下测试：

```js
// ----------------------------------------------------------------
// Phase 6: wecom + email 渠道 (NOTF-03, NOTF-04)
// ----------------------------------------------------------------

test('wecom channel formats markdown body correctly', async () => {
  const { NotificationService } = require('../src/service/notification-service');
  const service = new NotificationService();
  let capturedBody = null;
  service._webhookSender = async (target, payload) => {
    capturedBody = target.config;
    return { statusCode: 200 };
  };
  await service.send({
    type: 'test_event',
    level: 'warn',
    title: 'Test Title',
    message: 'Test message',
    targets: [{ channel: 'wecom', url: 'http://example.com/wecom' }],
  });
  assert.ok(capturedBody, 'body should be captured');
  assert.equal(capturedBody.msgtype, 'markdown');
  assert.ok(capturedBody.markdown.content.includes('Test Title'));
});

test('email channel throws when url or to is missing', async () => {
  const { NotificationService } = require('../src/service/notification-service');
  const service = new NotificationService();
  await assert.rejects(
    () => service.send({
      type: 'test',
      level: 'info',
      title: 'T',
      message: 'M',
      targets: [{ channel: 'email' }],
    }),
    /url|to/i,
  );
});

test('dedup returns deduplicated for same event within window (NOTF-05)', async () => {
  const { NotificationService } = require('../src/service/notification-service');
  const service = new NotificationService({ dedupWindowMs: 60000 });
  service._webhookSender = async () => ({ statusCode: 200 });
  const first = await service.send({
    type: 'low_balance',
    level: 'warn',
    title: 'Low Balance',
    message: 'Balance is low',
    targets: [{ channel: 'webhook', url: 'http://example.com' }],
  });
  const second = await service.send({
    type: 'low_balance',
    level: 'warn',
    title: 'Low Balance',
    message: 'Balance is low',
    targets: [{ channel: 'webhook', url: 'http://example.com' }],
  });
  assert.equal(first.sent, true);
  assert.equal(second.sent, false);
  assert.equal(second.reason, 'deduplicated');
});

test('dedup does not suppress different levels of same event type (NOTF-05)', async () => {
  const { NotificationService } = require('../src/service/notification-service');
  const service = new NotificationService({ dedupWindowMs: 60000 });
  service._webhookSender = async () => ({ statusCode: 200 });
  const warn = await service.send({
    type: 'low_balance',
    level: 'warn',
    title: 'Low Balance',
    message: 'Balance is low',
    targets: [{ channel: 'webhook', url: 'http://example.com' }],
  });
  const critical = await service.send({
    type: 'low_balance',
    level: 'critical',
    title: 'Low Balance',
    message: 'Balance is low',
    targets: [{ channel: 'webhook', url: 'http://example.com' }],
  });
  assert.equal(warn.sent, true);
  assert.equal(critical.sent, true, 'critical should not be suppressed by warn dedup');
});
```
</action>

<acceptance_criteria>
- `test/notification-service.test.js` 新增 4 个测试
- wecom 格式测试验证 msgtype 和 markdown.content
- email 缺少参数时抛出错误
- NOTF-05 去重测试：同级别被去重，不同级别不互相抑制
</acceptance_criteria>

### Task 6: 补充 test/refuel-orchestrator.test.js 测试

<read_first>
- test/refuel-orchestrator.test.js — 现有测试，makeOrchestrator helper 模式
</read_first>

<action>
在 `test/refuel-orchestrator.test.js` 末尾追加以下测试：

```js
// ----------------------------------------------------------------
// Phase 6: 免费模型降级 (FUEL-03)
// ----------------------------------------------------------------

test('prepareSession degrades to freeModel when balance is zero', async () => {
  const { RefuelOrchestrator } = require('../src/service/refuel-orchestrator');
  const { BudgetGuard } = require('../src/core/budget-guard');
  const { ModelSelector } = require('../src/core/model-selector');

  const adapter = {
    getBalance: async () => ({ availableUsd: 0, balanceUsd: 0 }),
    getUsage: async () => ({ dailySpentUsd: 0, hourlyTokensUsed: 0, autoRefuelsToday: 0, autoPurchasedUsdToday: 0 }),
    redeemCode: async () => ({}),
    issueKey: async () => ({ apiKey: 'test-key' }),
    renderDocs: async () => '',
  };
  const budgetGuard = new BudgetGuard({});
  const modelSelector = new ModelSelector({});
  const orch = new RefuelOrchestrator({
    adapter,
    budgetGuard,
    modelSelector,
    freeModel: 'gemini-2.5-flash-free',
  });

  const result = await orch.prepareSession({});
  assert.equal(result.degraded, true, 'should be degraded when balance is zero');
  assert.equal(result.selectedModel, 'gemini-2.5-flash-free', 'should use freeModel');
  assert.equal(result.status, 'ready', 'degraded session should be ready');
});

test('prepareSession does not degrade when balance is positive', async () => {
  const { RefuelOrchestrator } = require('../src/service/refuel-orchestrator');
  const { BudgetGuard } = require('../src/core/budget-guard');
  const { ModelSelector } = require('../src/core/model-selector');

  const adapter = {
    getBalance: async () => ({ availableUsd: 10, balanceUsd: 10 }),
    getUsage: async () => ({ dailySpentUsd: 0, hourlyTokensUsed: 0, autoRefuelsToday: 0, autoPurchasedUsdToday: 0 }),
    redeemCode: async () => ({}),
    issueKey: async () => ({ apiKey: 'test-key' }),
    renderDocs: async () => '',
  };
  const budgetGuard = new BudgetGuard({});
  const modelSelector = new ModelSelector({});
  const orch = new RefuelOrchestrator({
    adapter,
    budgetGuard,
    modelSelector,
    freeModel: 'gemini-2.5-flash-free',
  });

  const result = await orch.prepareSession({});
  assert.equal(result.degraded, false, 'should not be degraded when balance is positive');
});
```
</action>

<acceptance_criteria>
- `test/refuel-orchestrator.test.js` 新增 2 个测试
- balance=0 时 degraded=true，selectedModel=freeModel，status='ready'
- balance>0 时 degraded=false
</acceptance_criteria>

## Verification

```bash
node --test test/auto-refuel-decorator.test.js test/notification-service.test.js test/refuel-orchestrator.test.js
node --test test/*.test.js
```

期望：全部测试通过（203 + 新增约 10 个 = 213+ 通过）。
