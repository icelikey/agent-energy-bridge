---
phase: "07-open-source-release"
plan: "07-PLAN-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitleaks.toml
  - docs/security-scan-baseline.md
  - src/server/middleware/auth-middleware.js
  - test/auth-middleware.test.js
requirements: []
autonomous: false
tags: [security, gitleaks, secrets-scan, open-source-boundary]
must_haves:
  truths:
    - "仓库中无硬编码密码、API Key、Token（明文形式）"
    - "git 完整历史扫描完成，所有 finding 经评估（真实泄露 / false positive / 已撤销）"
    - "`.gitleaks.toml` 允许 `.env.example` 中的占位符（如 `your-api-key-here`）不被报错"
    - "`.gitleaks.toml` 允许 README/docs 中的 example.com 占位符不被报错"
    - "扫描结果记录到 docs/security-scan-baseline.md 供 SECURITY.md 引用（Wave 2）"
    - "Wave 2 文档任务在此扫描清单空（或全部 allowlisted）后才可启动"
    - "API Key 比较使用 crypto.timingSafeEqual() 恒时比较（Blocker 2 修复）"
  artifacts:
    - path: ".gitleaks.toml"
      provides: "gitleaks 允许列表配置"
      contains: "[allowlist]"
    - path: "docs/security-scan-baseline.md"
      provides: "首次扫描基线记录"
      min_lines: 20
    - path: "src/server/middleware/auth-middleware.js"
      provides: "timing-safe API Key 比较"
      contains: "crypto.timingSafeEqual"
    - path: "test/auth-middleware.test.js"
      provides: "timingSafeEqual 测试覆盖"
      contains: "timingSafeEqual"
  key_links:
    - from: ".gitleaks.toml"
      to: "Wave 3 CI workflow"
      via: "ci.yml secrets-scan job 引用此配置文件"
      pattern: "gitleaks/gitleaks-action@v2"
    - from: "src/server/middleware/auth-middleware.js"
      to: "test/auth-middleware.test.js"
      via: "timingSafeEqual 行为被测试覆盖"
      pattern: "crypto.timingSafeEqual"
---

<objective>
执行 Phase 7 的安全门槛（D-15）：使用 gitleaks 对 git 完整历史 + 工作区进行密钥扫描，识别并处置潜在私有信息（硬编码密钥、真实地址、私有配置）。同时建立 `.gitleaks.toml` 允许列表，使 `.env.example`、README 中的占位符（`your-api-key-here`、`example.com` 等）不触发误报。

> **Note (revision 2026-05-11):** 本 plan 实现 **D-15**（Phase 7 先决决策：开源前安全扫描），不对应任何 OPEN-XX 需求。CI 工作流（OPEN-03）由 PLAN-06 交付。本 plan 同时包含 Blocker 2 修复：将 PLAN-01 已 ship 的 timing-unsafe 比较升级为 `crypto.timingSafeEqual()`。

Purpose: 在开源发布（Wave 4 推送 ghcr.io、创建公开 Release）前堵住"私有信息进开源仓库"这一最高风险路径。CONTEXT.md D-15 明确要求"开源前的安全扫描任务"必须在 Phase 7 其他任务之前完成。同时，SECURITY.md（PLAN-04）声称使用 `crypto.timingSafeEqual()`，因此必须在 SECURITY.md 公布前将实际代码升级到位。

Output:
- `.gitleaks.toml`（gitleaks allowlist 配置文件，供 Wave 3 CI 引用）
- `docs/security-scan-baseline.md`（首次扫描的发现清单与处置记录）
- `src/server/middleware/auth-middleware.js`（升级 timingSafeEqual 比较）
- `test/auth-middleware.test.js`（新增 timingSafeEqual 测试用例）
- checkpoint：人工确认扫描结果，决定是否继续推进 Wave 2+
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-open-source-release/07-CONTEXT.md
@.planning/phases/07-open-source-release/07-RESEARCH.md
@.planning/phases/07-open-source-release/07-PLAN-01-SUMMARY.md
@src/server/middleware/auth-middleware.js
@.gitignore
@.env.example
@CLAUDE.md
@docs/open-source-release-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: 升级 api-key-auth middleware 为 crypto.timingSafeEqual（Blocker 2 修复）</name>
  <files>src/server/middleware/auth-middleware.js, test/auth-middleware.test.js</files>
  <read_first>
    - src/server/middleware/auth-middleware.js（PLAN-01 已 ship 的当前实现）
    - test/auth-middleware.test.js（PLAN-01 已 ship 的 4 个测试）
  </read_first>
  <action>
**Step 1 — 修改 `src/server/middleware/auth-middleware.js`**

用以下完整文件内容替换现有实现（零外部依赖，`crypto` 为 Node.js 内置模块）：

```js
/**
 * API Key authentication middleware.
 * Zero external dependencies.
 *
 * Protects management endpoints (e.g., POST /notify/test) with X-API-Key header.
 * When AEB_API_KEY env var is not configured, the middleware is a no-op to preserve
 * developer-friendly defaults.
 */

const crypto = require('node:crypto');

/**
 * Constant-time string comparison to prevent timing attacks.
 * Returns false if inputs are not strings or have different lengths.
 */
function safeEqual(a, b) {
  if (typeof a !== 'string' || typeof b !== 'string') return false;
  const ba = Buffer.from(a);
  const bb = Buffer.from(b);
  if (ba.length !== bb.length) return false;
  return crypto.timingSafeEqual(ba, bb);
}

function createAuthMiddleware(options = {}) {
  const apiKey = options.apiKey || process.env.AEB_API_KEY || null;

  return function authMiddleware(request, response, context) {
    // 未配置 API Key 时跳过认证（开发友好）
    if (!apiKey) return;

    const headers = request.headers || {};
    const providedKey = headers['x-api-key'] || null;

    if (!safeEqual(providedKey, apiKey)) {
      const error = new Error('Unauthorized: invalid or missing API key');
      error.statusCode = 401;
      error.code = 'UNAUTHORIZED';
      throw error;
    }
  };
}

module.exports = { createAuthMiddleware };
```

**关键变更说明：**
1. 新增 `require('node:crypto')` 导入（零外部依赖，Node.js 内置）
2. 新增 `safeEqual()` 函数：先做类型检查、长度检查，再用 `crypto.timingSafeEqual()` 做恒时比较
3. 移除冗余 OR：`headers['x-api-key'] || headers['x-api-key']` → 简化为 `headers['x-api-key']`（同时修复 Warning 6）
4. 比较逻辑从 `providedKey !== apiKey` 改为 `!safeEqual(providedKey, apiKey)`
5. 错误对象格式保持不变（statusCode=401, code='UNAUTHORIZED'）

**Step 2 — 扩展 `test/auth-middleware.test.js`**

在现有 4 个测试之后追加 2 个新测试：

```js
test('auth middleware rejects key with different length (timing-safe)', () => {
  const middleware = createAuthMiddleware({ apiKey: 'secret-123' });
  assert.throws(
    () => middleware({ headers: { 'x-api-key': 'secret-123-extra' } }, {}, {}),
    { statusCode: 401, code: 'UNAUTHORIZED' }
  );
});

test('auth middleware accepts correct key via timingSafeEqual', () => {
  const middleware = createAuthMiddleware({ apiKey: 'a'.repeat(64) });
  assert.doesNotThrow(
    () => middleware({ headers: { 'x-api-key': 'a'.repeat(64) } }, {}, {})
  );
});
```

**Step 3 — 验证**

```bash
cd H:/projects/smart-relay-station
node --test test/auth-middleware.test.js
```

期望：6 个测试全部通过（原有 4 个 + 新增 2 个）。
  </action>
  <verify>
    <automated>cd H:/projects/smart-relay-station && grep -q "crypto.timingSafeEqual" src/server/middleware/auth-middleware.js && grep -q "safeEqual" src/server/middleware/auth-middleware.js && node --test test/auth-middleware.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/middleware/auth-middleware.js` 包含 `require('node:crypto')`
    - `src/server/middleware/auth-middleware.js` 包含 `crypto.timingSafeEqual`
    - `src/server/middleware/auth-middleware.js` 不再包含 `providedKey !== apiKey`（timing-unsafe 比较已移除）
    - `src/server/middleware/auth-middleware.js` 不再包含冗余 OR `headers['x-api-key'] || headers['x-api-key']`
    - `test/auth-middleware.test.js` 新增 2 个测试（不同长度 key 拒绝、64 字符 key 通过）
    - `node --test test/auth-middleware.test.js` 6/6 通过
    - `node --test test/*.test.js` 全部通过（无回归）
  </acceptance_criteria>
  <done>
    API Key 比较升级为 `crypto.timingSafeEqual()` 恒时比较，冗余 OR 表达式已清理，测试覆盖新增 2 个用例。
  </done>
</task>

<task type="auto">
  <name>Task 1: 创建 .gitleaks.toml 允许列表配置</name>
  <files>.gitleaks.toml</files>
  <read_first>
    - .env.example（确认占位符字面量，如 `your-api-key-here`、`your-password`、`your-newapi-server`）
    - README.md（确认 example.com、gateway.example.com 等占位符）
    - .gitignore（确认 docs/reseller-pack/、docs/deployments/、docs/private-*.md 已被排除）
    - .planning/phases/07-open-source-release/07-RESEARCH.md §Pattern 1 & §Pitfall 5（gitleaks 配置规范、占位符 false positive 处置）
  </read_first>
  <action>
在仓库根目录创建 `.gitleaks.toml`（字面量内容，逐字写入）：

```toml
# gitleaks 配置 — Agent Energy Bridge
# 用途：在 CI 中扫描密钥泄露，同时允许文档/示例中的占位符不被误报
# Source: https://github.com/gitleaks/gitleaks/blob/master/config/gitleaks.toml

title = "Agent Energy Bridge — gitleaks config"

# 使用 gitleaks 默认规则集为基础
[extend]
useDefault = true

[allowlist]
description = "允许文档/示例中的占位符与已废弃的测试密钥"

# 文件级豁免：以下路径中的任何高熵字符串都视为占位符
paths = [
    '''^\.env\.example$''',
    '''^README\.md$''',
    '''^README\.en\.md$''',
    '''^docs/.*\.md$''',
    '''^docs/API\.md$''',
    '''^CONTRIBUTING\.md$''',
    '''^SECURITY\.md$''',
    '''^CHANGELOG\.md$''',
    '''^docker-compose\.yml$''',
    '''^test/.*\.test\.js$''',
    '''^tests/.*\.mjs$''',
]

# 通用占位符正则（即使在 src/ 中出现也视为安全）
regexes = [
    '''your-(api-key|password|username|secret|token)-here''',
    '''your-newapi-server''',
    '''<YOUR_[A-Z_]+>''',
    '''example\.com''',
    '''gateway\.example\.com''',
    '''api\.example\.com''',
    '''agent\.example\.com''',
    '''ak-demo''',
    '''sess-123''',
    '''DEMO-2026''',
    '''CODE[123]''',
    '''RECHARGE-[0-9]+''',
    '''secret-123''',
    '''ghcr\.io/\{owner\}''',
    '''github\.com/your-org''',
    '''security@your-org\.com''',
]

# 已知 commit 豁免（如有历史泄露已撤销）— 初始为空，扫描后视情况追加
commits = []
```

注意：
- 字段名严格使用 `useDefault = true`（不要写 `useDefault = "true"`，会被解析为字符串）。
- `paths` 中使用 `'''...'''` 三引号原始字符串以避免转义反斜杠。
- 不要写 `[rules]` 自定义规则；本配置只做 allowlist。
  </action>
  <verify>
    <automated>test -f .gitleaks.toml &amp;&amp; grep -q '^useDefault = true' .gitleaks.toml &amp;&amp; grep -q 'your-api-key-here' .gitleaks.toml &amp;&amp; grep -q 'example\.com' .gitleaks.toml</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `.gitleaks.toml` 存在于仓库根目录
    - 包含字面量 `title = "Agent Energy Bridge — gitleaks config"`
    - 包含 `[extend]` 段且 `useDefault = true`
    - 包含 `[allowlist]` 段
    - `paths` 列表至少包含 `\.env\.example$`、`README\.md$`、`docs/.*\.md$`、`docker-compose\.yml$`、`test/.*\.test\.js$` 五个条目
    - `regexes` 列表至少包含 `your-(api-key|password|username|secret|token)-here`、`example\.com`、`<YOUR_[A-Z_]+>`、`ak-demo`、`DEMO-2026` 五个条目
    - 文件无语法错误（可被 gitleaks 加载，不抛出 TOML parse error）
  </acceptance_criteria>
  <done>
    `.gitleaks.toml` 写入仓库根目录，allowlist 覆盖 .env.example 占位符与所有文档/测试中的示例字面量。
  </done>
</task>

<task type="auto">
  <name>Task 2: 运行 gitleaks 全量历史扫描并记录结果</name>
  <files>docs/security-scan-baseline.md</files>
  <read_first>
    - .gitleaks.toml（Task 1 刚创建的允许列表）
    - .planning/phases/07-open-source-release/07-RESEARCH.md §Pitfall 9（git 历史泄露处置流程）
    - .planning/phases/07-open-source-release/07-RESEARCH.md §Runtime State Inventory（已知扫描重点区域）
  </read_first>
  <action>
执行以下扫描命令（按顺序），并将每一步结果写入 `docs/security-scan-baseline.md`：

**前置准备**（如果本地无 gitleaks 二进制）：
```bash
# Windows：winget install gitleaks
# macOS/Linux：brew install gitleaks  或  curl -L https://github.com/gitleaks/gitleaks/releases/latest/download/gitleaks_linux_x64.tar.gz | tar xz
# 验证：gitleaks version  → 应输出 v8.x.x
```

**Step 1：扫描当前工作区**
```bash
cd H:/projects/smart-relay-station
gitleaks detect --config .gitleaks.toml --no-banner --redact --report-format json --report-path tmp-gitleaks-worktree.json --source .
```
预期：exit code 0（无 finding）或 1（有 finding，需评估）。

**Step 2：扫描完整 git 历史**
```bash
gitleaks detect --config .gitleaks.toml --no-banner --redact --log-opts="--all" --report-format json --report-path tmp-gitleaks-history.json --source .
```
预期：exit code 0 或 1。

**Step 3：将 finding 分类**
对每条 finding，归入下列分类之一：
- **REAL_LEAK**：真实泄露的密钥（生产环境曾使用） → 必须 rotate + 评估是否 `git filter-repo` 重写历史
- **REVOKED**：早期硬编码已在 Phase 1 SECU-01 移除，密钥已撤销 → 记录但无需重写历史
- **FALSE_POSITIVE_ENV_EXAMPLE**：`.env.example` 占位符（理论上已被 allowlist 拦截）→ 更新 `.gitleaks.toml` 补 allowlist
- **FALSE_POSITIVE_DOC**：文档/README 中的示例 → 更新 `.gitleaks.toml` 补 allowlist
- **TEST_FIXTURE**：测试夹具中的虚假密钥（`ak-demo`、`secret-123` 等）→ 已被 allowlist 拦截
- **UNCLEAR**：需人工判断 → 留给 Task 3 checkpoint

**Step 4：写入 `docs/security-scan-baseline.md`**（字面量结构）：

```markdown
# Security Scan Baseline

**Date:** {YYYY-MM-DD 实际执行日期}
**Tool:** gitleaks v{实际版本}
**Config:** `.gitleaks.toml`
**Scanned:** working tree + full git history (`--log-opts=--all`)

## Worktree Scan Result

| Metric | Value |
|--------|-------|
| Total findings | {N} |
| REAL_LEAK | {n1} |
| REVOKED | {n2} |
| FALSE_POSITIVE | {n3} |
| TEST_FIXTURE | {n4} |
| UNCLEAR | {n5} |

## History Scan Result

| Metric | Value |
|--------|-------|
| Total findings | {N} |
| REAL_LEAK | {n1} |
| REVOKED | {n2} |
| FALSE_POSITIVE | {n3} |
| TEST_FIXTURE | {n4} |
| UNCLEAR | {n5} |

## Findings Detail

### REAL_LEAK
{若无：写 "None — 仓库无真实泄露的密钥。"；若有：列出每条 finding 的 file, line, rule, commit, 处置建议}

### REVOKED
{若有：列出来自 Phase 1 SECU-01 修复前的硬编码密码 / Math.random API Key，并标注"已撤销，无需重写历史"}

### FALSE_POSITIVE
{列出 allowlist 已覆盖或新增 allowlist 覆盖的条目}

### TEST_FIXTURE
{列出 ak-demo、secret-123、DEMO-2026 等测试字面量，allowlist 已覆盖}

### UNCLEAR (需人工评估)
{留给 Task 3 checkpoint 决策}

## Allowlist Updates Applied

{若 Task 2 过程中更新了 .gitleaks.toml allowlist，列出新增条目；否则写 "无更新"}

## Outcome

{以下三选一}
- [ ] **PASS** — 无 REAL_LEAK 且无 UNCLEAR，Wave 2+ 可继续推进
- [ ] **PASS_WITH_NOTE** — 有 REVOKED finding，但无 REAL_LEAK，已记录到 SECURITY.md（Wave 2）
- [ ] **BLOCKED** — 存在 REAL_LEAK 或 UNCLEAR 需 checkpoint 决策
```

**Step 5：清理临时文件**
```bash
rm -f tmp-gitleaks-worktree.json tmp-gitleaks-history.json
```
（不要将 JSON 报告 commit 到仓库；它们可能含 redact 后仍可识别的部分密钥碎片。）

如果 gitleaks 二进制不可用（环境限制），则改用 fallback 手动 grep 规则（基于 RESEARCH.md §Runtime State Inventory）：
```bash
# fallback：手动扫描已知高风险词
grep -nr -E '(password|secret|api[_-]?key|token)\s*[:=]\s*["\047][a-zA-Z0-9]{16,}["\047]' --include='*.js' --include='*.md' --include='*.yml' --include='*.json' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning . > tmp-grep-scan.txt 2>/dev/null || true
grep -nr -E 'https?://[a-zA-Z0-9.-]+\.(com|net|org|cn|io)' --include='*.js' --exclude-dir=node_modules --exclude-dir=.git . | grep -vE '(example\.com|localhost|127\.0\.0\.1|github\.com|docker\.io|ghcr\.io)' > tmp-grep-urls.txt 2>/dev/null || true
# 评估两个文件内容，按分类写入 docs/security-scan-baseline.md，然后删除
rm -f tmp-grep-scan.txt tmp-grep-urls.txt
```
fallback 模式下，`docs/security-scan-baseline.md` 顶部需注明：**"Tool: manual grep (gitleaks binary unavailable in environment)"**，并在 Task 3 checkpoint 中明示"建议在 CI 中用 gitleaks-action 进行二次验证（Wave 3 已包含此 job）"。
  </action>
  <verify>
    <automated>test -f docs/security-scan-baseline.md &amp;&amp; grep -q '^# Security Scan Baseline' docs/security-scan-baseline.md &amp;&amp; grep -qE '^## Outcome' docs/security-scan-baseline.md &amp;&amp; grep -qE '(PASS|PASS_WITH_NOTE|BLOCKED)' docs/security-scan-baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/security-scan-baseline.md` 存在
    - 包含 5 个 H2 标题：`## Worktree Scan Result`、`## History Scan Result`、`## Findings Detail`、`## Allowlist Updates Applied`、`## Outcome`
    - `## Findings Detail` 下至少包含 5 个 H3 子标题：`### REAL_LEAK`、`### REVOKED`、`### FALSE_POSITIVE`、`### TEST_FIXTURE`、`### UNCLEAR (需人工评估)`
    - `## Outcome` 选项之一被勾选（PASS / PASS_WITH_NOTE / BLOCKED）
    - 仓库根目录无 `tmp-gitleaks-*.json` 或 `tmp-grep-*.txt` 残留
    - `docs/security-scan-baseline.md` 中**不包含**完整明文密钥（任何看似密钥的字段必须 redact 或仅记录 commit hash + 文件路径）
  </acceptance_criteria>
  <done>
    扫描执行完毕，结果分类记录到 baseline 文档，临时报告已清理；扫描结论（PASS / BLOCKED）明确写入 Outcome 段。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — 人工确认扫描结果，决定是否继续 Wave 2+</name>
  <what-built>
    Task 0 升级了 auth-middleware.js 使用 `crypto.timingSafeEqual()`，并新增 2 个测试用例。
    Task 1 创建了 `.gitleaks.toml`（gitleaks 允许列表配置）。
    Task 2 执行了完整工作区 + git 历史扫描，结果分类记录在 `docs/security-scan-baseline.md`。
    所有 finding 已按 REAL_LEAK / REVOKED / FALSE_POSITIVE / TEST_FIXTURE / UNCLEAR 分类。
  </what-built>
  <how-to-verify>
    1. 打开 `docs/security-scan-baseline.md`，确认 `## Outcome` 段的选择：
       - 若勾选 **PASS** → 直接批准 Wave 2 启动
       - 若勾选 **PASS_WITH_NOTE** → 确认 REVOKED finding 已被记录（Wave 2 PLAN-04 SECURITY.md 中会引用），批准 Wave 2 启动
       - 若勾选 **BLOCKED** → 进入决策流程：
         a. 真实泄露的密钥：**必须先 rotate**（在对应服务方撤销旧 key），然后决定是否 `git filter-repo` 重写历史
         b. UNCLEAR finding：人工逐条评估并改分类，更新 baseline 文档后再次进入此 checkpoint
    2. 检查 `.gitleaks.toml` 是否合理：
       - allowlist `paths` 不要过于宽松（如 `^.*$` 会完全禁用扫描）
       - allowlist `regexes` 不要包含真实密钥模式
    3. 验证仓库根目录无 `tmp-gitleaks-*.json` / `tmp-grep-*.txt` 残留：
       ```bash
       ls H:/projects/smart-relay-station/tmp-* 2>/dev/null && echo "残留文件需删除" || echo "OK"
       ```
    4. 若 Task 2 使用了 fallback 手动 grep（gitleaks 不可用），确认已在 baseline 顶部标注，且接受 Wave 3 CI 中的 gitleaks-action 作为二次验证。
    5. 验证 timingSafeEqual 升级：
       ```bash
       cd H:/projects/smart-relay-station
       node --test test/auth-middleware.test.js
       ```
       期望 6/6 通过。
  </how-to-verify>
  <acceptance_criteria>
    - `docs/security-scan-baseline.md` Outcome 已被人工确认（PASS / PASS_WITH_NOTE / BLOCKED 之一被勾选）
    - 若 BLOCKED，已记录后续行动项（rotate 哪个 key、是否 filter-repo 等）
    - 仓库根目录无临时报告文件残留
    - `node --test test/auth-middleware.test.js` 6/6 通过
    - 用户明确批准 Wave 2 启动（或要求暂停 Phase 7 进入紧急讨论流程）
  </acceptance_criteria>
  <resume-signal>
    输入 "approved" 批准 Wave 2 启动；输入 "blocked: {原因}" 暂停 Phase 7 并启动 `/gsd-discuss-phase` 讨论后续动作。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 仓库公开化 | 私有 git history → 公开 GitHub 仓库；任何当前隐藏的密钥都会被全世界 `git log -S 'pattern'` 检索到 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | git history | mitigate | Task 2 全历史扫描 + Task 3 checkpoint 人工评估；发现真实泄露则 rotate + 评估 filter-repo（per D-15） |
| T-07-02 | Information Disclosure | `.env.example`、README、docs/*.md | accept | 已知含占位符（your-api-key-here、example.com）；.gitleaks.toml allowlist 覆盖（per D-15） |
| T-07-03 | Spoofing | `.gitleaks.toml` allowlist 过度宽松 | mitigate | Task 3 checkpoint 中人工审核 allowlist 不包含真实密钥模式 |
| T-07-04 | Information Disclosure | gitleaks JSON 报告文件 | mitigate | Task 2 执行后立即 `rm` 临时报告；不进 git history |
| T-07-05 | Information Disclosure | timing attack on API key comparison | mitigate | Task 0 升级 `providedKey !== apiKey` → `crypto.timingSafeEqual()`；长度不匹配时提前返回 false（不暴露长度信息） |
</threat_model>

<verification>
本 Phase 7 Plan 02 完成时：

```bash
# 1. timingSafeEqual 升级已到位
grep -q "crypto.timingSafeEqual" src/server/middleware/auth-middleware.js
! grep -q "providedKey !== apiKey" src/server/middleware/auth-middleware.js

# 2. .gitleaks.toml 存在且语法正确
test -f .gitleaks.toml && grep -q '^useDefault = true' .gitleaks.toml

# 3. security-scan-baseline.md 存在且 Outcome 已勾选
test -f docs/security-scan-baseline.md && grep -qE '\[x\].*(PASS|PASS_WITH_NOTE|BLOCKED)' docs/security-scan-baseline.md

# 4. 仓库根目录无临时报告残留
ls tmp-gitleaks-* tmp-grep-* 2>/dev/null | wc -l  # 应为 0

# 5. 测试通过（6 auth + 其余 = 219 预期）
node --test
```

期望：测试 219/219 通过（auth-middleware 6 个 + 其余 213 个），无 tmp 残留，baseline Outcome 已确认。
</verification>

<success_criteria>
- `src/server/middleware/auth-middleware.js` 使用 `crypto.timingSafeEqual()` 进行恒时比较
- `test/auth-middleware.test.js` 覆盖 timingSafeEqual 行为（6 个测试）
- `.gitleaks.toml` 创建完成，allowlist 覆盖所有已知占位符
- `docs/security-scan-baseline.md` 完整记录工作区 + 全历史扫描结果
- 人工确认无 REAL_LEAK（或已识别并启动 rotate 流程）
- Wave 2 各 plan 可基于此扫描清单开始执行
- D-15 完成（"开源前的安全扫描任务"）
- Wave 3 CI workflow（PLAN-06）可直接引用 `.gitleaks.toml` 作为 gitleaks-action 配置
</success_criteria>

<output>
完成后创建 `.planning/phases/07-open-source-release/07-PLAN-02-SUMMARY.md`，包含：
- timingSafeEqual 升级摘要
- 扫描发现统计（按分类）
- `.gitleaks.toml` 关键 allowlist 条目摘要
- Outcome 决策
- 对 Wave 2 / Wave 3 的下游影响
</output>
