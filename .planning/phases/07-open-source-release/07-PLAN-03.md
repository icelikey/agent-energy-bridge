---
phase: "07-open-source-release"
plan: "07-PLAN-03"
type: execute
wave: 2
depends_on: ["07-PLAN-02"]
files_modified:
  - docs/API.md
requirements: [OPEN-02]
autonomous: true
tags: [docs, api, bilingual, open-source]
must_haves:
  truths:
    - "docs/API.md 覆盖全部 26 个 router.js 中注册的端点"
    - "每个端点至少包含：方法+路径、请求参数（body 或 query）、响应示例、错误码"
    - "中英双语呈现（每端点的描述同时含中英文）"
    - "POST /agent/v1/notify/test 明确标注需 X-API-Key 认证"
    - "无任何真实地址、生产域名、真实密钥"
    - "示例 URL 一律使用 example.com / localhost 占位符"
  artifacts:
    - path: "docs/API.md"
      provides: "完整 API 参考手册（26 endpoints, bilingual）"
      min_lines: 500
      contains: "## POST /agent/v1/optimize"
  key_links:
    - from: "docs/API.md"
      to: "README.md / README.en.md"
      via: "README 引用 docs/API.md 链接"
      pattern: "docs/API\\.md"
    - from: "src/server/router.js ROUTES"
      to: "docs/API.md 端点章节"
      via: "1-to-1 mapping（每个 ROUTES 条目对应 1 个 docs section）"
      pattern: "POST|GET /agent/v1/"
---

<objective>
创建 `docs/API.md`：Agent Energy Bridge 全部 26 个 HTTP 端点的中英双语 API 参考手册。每个端点遵循统一模板（方法+路径、Auth 标记、Request、Response、Errors、Example curl）。这是 OPEN-02 的核心交付物，由用户在 D-01/D-02/D-03 中锁定为"Markdown 手写、中英双语、含标准字段"格式。

Purpose: 让开源用户（包括非中文母语用户）能不依赖代码阅读直接对接所有 endpoint；同时让 docs/API.md 成为 D-14 X-API-Key 认证策略的对外文档。

Output: 单一文件 `docs/API.md`，约 500-800 行，覆盖 26 个端点全部字段。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-open-source-release/07-CONTEXT.md
@.planning/phases/07-open-source-release/07-RESEARCH.md
@src/server/router.js
@src/server/handlers/health.js
@src/server/handlers/balance.js
@src/server/handlers/usage.js
@src/server/handlers/capabilities.js
@src/server/handlers/recommend.js
@src/server/handlers/optimize.js
@src/server/handlers/refuel.js
@src/server/handlers/keys.js
@src/server/handlers/docs.js
@src/server/handlers/session-report.js
@src/server/handlers/session-summary.js
@src/server/handlers/ops.js
@src/server/handlers/meter.js
@src/server/handlers/routing.js
@src/server/handlers/notify.js
@src/server/handlers/refuel-status.js

<interfaces>
<!-- 26 endpoints from src/server/router.js ROUTES (verified 2026-05-11). 编写 docs/API.md 时，必须为以下每条都生成一个独立章节： -->

1. GET  /agent/v1/health                  → handlers/health.js getHealth
2. GET  /agent/v1/balance                 → handlers/balance.js getBalance
3. GET  /agent/v1/usage/summary           → handlers/usage.js getUsageSummary
4. GET  /agent/v1/models/capabilities     → handlers/capabilities.js getCapabilities
5. POST /agent/v1/recommend               → handlers/recommend.js postRecommend
6. POST /agent/v1/optimize                → handlers/optimize.js postOptimize
7. POST /agent/v1/refuel/redeem           → handlers/refuel.js postRefuelRedeem
8. POST /agent/v1/keys/issue              → handlers/keys.js postIssueKey
9. POST /agent/v1/docs/render             → handlers/docs.js postRenderDocs
10. POST /agent/v1/session/report         → handlers/session-report.js postSessionReport
11. GET  /agent/v1/session/summary        → handlers/session-summary.js getSessionSummary
12. GET  /agent/v1/ops/snapshot           → handlers/ops.js getOpsSnapshot
13. GET  /agent/v1/ops/report             → handlers/ops.js getOpsReport
14. GET  /agent/v1/ops/energy             → handlers/ops.js getOpsEnergy
15. POST /agent/v1/ops/start              → handlers/ops.js postOpsStart
16. POST /agent/v1/ops/stop               → handlers/ops.js postOpsStop
17. POST /agent/v1/meter/record           → handlers/meter.js postMeterRecord
18. GET  /agent/v1/meter/stats            → handlers/meter.js getMeterStats
19. GET  /agent/v1/meter/report           → handlers/meter.js getMeterReport
20. GET  /agent/v1/meter/breakdown        → handlers/meter.js getMeterBreakdown
21. GET  /agent/v1/routing/status         → handlers/routing.js getRoutingStatus
22. GET  /agent/v1/routing/log            → handlers/routing.js getRoutingLog
23. POST /agent/v1/routing/force          → handlers/routing.js postRoutingForce
24. GET  /agent/v1/notify/config          → handlers/notify.js getNotifyConfig
25. POST /agent/v1/notify/test            → handlers/notify.js postNotifyTest  (X-API-Key REQUIRED)
26. GET  /agent/v1/refuel/status          → handlers/refuel-status.js getRefuelStatus

⚠️ 实际端点数为 **26**（不是 CONTEXT.md 中说的 16）。Phase 4 (meter)、Phase 5 (routing)、Phase 6 (notify/refuel-status) 又新增了端点。在 docs/API.md 中必须**全部覆盖**这 26 个端点。docs/API.md 文档顶部应标注："本文档覆盖 26 个端点（截至 v0.1.0）。"

<!-- 错误响应统一格式（见 src/server/middleware/error-handler.js）: -->
```json
{
  "success": false,
  "error": "Error class name",
  "message": "Human-readable description",
  "code": "ERROR_CODE",
  "statusCode": 400
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证全部 26 个端点的请求/响应字段（先读源码做内部目录）</name>
  <files>
    （无文件修改；这是 information-gathering 子任务）
  </files>
  <read_first>
    - src/server/router.js（ROUTES 数组，确认端点列表）
    - src/server/handlers/health.js, balance.js, usage.js, capabilities.js（GET 端点）
    - src/server/handlers/recommend.js, optimize.js, refuel.js, keys.js, docs.js, session-report.js（POST 端点）
    - src/server/handlers/session-summary.js, ops.js（混合端点）
    - src/server/handlers/meter.js, routing.js, notify.js, refuel-status.js（Phase 4/5/6 端点）
    - src/server/middleware/error-handler.js（错误响应字段名规范）
  </read_first>
  <action>
逐个 handler 文件读取，提取 **每个端点的**：
- **请求 body 字段**（POST/PUT）：从 `const body = request.body || {}; const x = body.x;` 模式中提取
- **请求 query 字段**（GET）：从 `request.query.x` 模式中提取
- **响应字段**：从 `return { ... }` 提取（注意嵌套对象）
- **错误码**：从 `error.code = '...'` 提取
- **HTTP 状态**：从 `error.statusCode = ...` 提取（默认 200）

将提取结果整理为一份临时纸面笔记（不写入仓库），用于 Task 2 的填充。

**关键约束**：
- 不要"想象"响应字段；必须实际读源码确认
- 如果某 handler 调用了 `context.adapter.getBalance()` 等下游服务，按下游服务的真实返回字段记录
- ops.js 含 5 个端点（snapshot / report / energy / start / stop）；meter.js 含 4 个；routing.js 含 3 个；notify.js 含 2 个
  </action>
  <verify>
    <automated>node -e "const r=require('./src/server/router').ROUTES; if(r.length!==26){process.exit(1)} console.log('routes='+r.length)"</automated>
  </verify>
  <acceptance_criteria>
    - 已阅读 router.js 并确认 ROUTES 包含 26 个端点
    - 已读取 16 个 handler 文件（health, balance, usage, capabilities, recommend, optimize, refuel, keys, docs, session-report, session-summary, ops, meter, routing, notify, refuel-status）
    - 已建立每端点的字段清单（用于 Task 2）
  </acceptance_criteria>
  <done>
    内部端点字段清单完成，可供 Task 2 直接写入 docs/API.md。
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 docs/API.md（中英双语，26 端点完整覆盖）</name>
  <files>docs/API.md</files>
  <read_first>
    - Task 1 的端点字段清单
    - .planning/phases/07-open-source-release/07-RESEARCH.md §"Example: API.md 端点条目格式"
    - .planning/phases/07-open-source-release/07-CONTEXT.md D-01/D-02/D-03
  </read_first>
  <action>
创建 `docs/API.md`，整体结构如下（逐字按本模板写入）：

```markdown
# Agent Energy Bridge — HTTP API Reference

> [中文](#中文版) | [English](#english-version)

**Version:** v0.1.0
**Base URL:** `http://localhost:3100` (default; configurable via `AEB_PORT` and `AEB_HOST`)
**Total endpoints:** 26
**Content-Type:** `application/json` (all POST/PUT requests)
**Encoding:** UTF-8

---

## English Version

### Table of Contents

- [Authentication](#authentication)
- [Error Response Format](#error-response-format)
- [Endpoints](#endpoints)
  - [Health & Status](#health--status) — 1 endpoint
  - [Balance & Usage](#balance--usage) — 2 endpoints
  - [Models & Recommendations](#models--recommendations) — 3 endpoints
  - [Refuel & Keys](#refuel--keys) — 3 endpoints
  - [Sessions](#sessions) — 2 endpoints
  - [Ops Monitoring](#ops-monitoring) — 5 endpoints
  - [Token Metering](#token-metering) — 4 endpoints
  - [Multi-Provider Routing](#multi-provider-routing) — 3 endpoints
  - [Notifications](#notifications) — 2 endpoints
  - [Refuel Status](#refuel-status) — 1 endpoint

### Authentication

Most endpoints are **unauthenticated** by default. Admin endpoints that mutate state or trigger external side effects require an `X-API-Key` HTTP header:

| Endpoint | Auth Required |
|----------|---------------|
| `POST /agent/v1/notify/test` | **Yes** (`X-API-Key`) |

To enable auth, set the `AEB_API_KEY` environment variable. If `AEB_API_KEY` is **not set**, the auth middleware acts as a no-op (backward compatible for existing deployments).

```bash
export AEB_API_KEY=$(openssl rand -hex 32)
curl -H "X-API-Key: $AEB_API_KEY" http://localhost:3100/agent/v1/notify/test
```

### Error Response Format

All errors follow a uniform JSON structure (see `src/server/middleware/error-handler.js`):

```json
{
  "success": false,
  "error": "Error",
  "message": "Human-readable description",
  "code": "ERROR_CODE",
  "statusCode": 400
}
```

| HTTP Status | Common Codes |
|-------------|--------------|
| 400 | `MISSING_FIELD`, `INVALID_VALUE`, `MISSING_URL`, `INVALID_CODE_FORMAT` |
| 401 | `UNAUTHORIZED` (X-API-Key missing or wrong) |
| 403 | `RATE_LIMITED` |
| 404 | `NOT_FOUND` |
| 503 | `SERVICE_NOT_CONFIGURED` (adapter/service not initialized) |

### Endpoints

#### Health & Status

##### GET /agent/v1/health

Service health check / 服务健康检查

**Auth:** Not required
**Method:** `GET`

**Response (200):**

\`\`\`json
{
  "status": "ok",
  "timestamp": "2026-05-11T04:23:31.653Z",
  "version": "0.1.0"
}
\`\`\`

**Example:**

\`\`\`bash
curl http://localhost:3100/agent/v1/health
\`\`\`

---

#### Balance & Usage

##### GET /agent/v1/balance

Query current account balance / 查询账户余额

**Auth:** Not required
**Method:** `GET`

**Response (200):**

\`\`\`json
{
  "availableUsd": 10.5,
  "balanceUsd": 10.5,
  "currency": "USD"
}
\`\`\`

**Errors:**

| Code | Status | Meaning |
|------|--------|---------|
| `SERVICE_NOT_CONFIGURED` | 503 | Adapter not configured / 网关未配置 |

**Example:**

\`\`\`bash
curl http://localhost:3100/agent/v1/balance
\`\`\`

---

##### GET /agent/v1/usage/summary

{... 同样模板，按 Task 1 提取的实际字段填充 ...}

---

{... 重复 24 次，覆盖剩余 24 个端点；每个端点至少包含 4 段：描述 / Auth / Request / Response / Errors / Example ...}

---

##### POST /agent/v1/notify/test

Send a test notification through configured channels / 发送测试通知

**Auth:** **Required** (`X-API-Key` header)
**Method:** `POST`

**Request Body:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `channel` | string | No | Specific channel: `webhook` / `feishu` / `dingtalk` / `slack` / `wecom` / `email` / 通知渠道 |
| `url` | string | Conditional | Required when `channel` is specified / 指定 channel 时必填 |
| `level` | string | No | Severity: `info` / `warn` / `critical` (default `info`) |
| `title` | string | No | Notification title / 通知标题 |
| `message` | string | No | Notification body / 通知正文 |

**Response (200):**

\`\`\`json
{
  "sent": ["webhook", "feishu"],
  "failed": [],
  "skipped": ["dingtalk"]
}
\`\`\`

**Errors:**

| Code | Status | Meaning |
|------|--------|---------|
| `UNAUTHORIZED` | 401 | Missing or invalid `X-API-Key` header / 缺失或错误的 API Key |
| `MISSING_URL` | 400 | `channel` specified but no `url` provided |
| `SERVICE_NOT_CONFIGURED` | 503 | NotificationService not initialized |

**Example:**

\`\`\`bash
curl -X POST http://localhost:3100/agent/v1/notify/test \\
  -H "X-API-Key: $AEB_API_KEY" \\
  -H "Content-Type: application/json" \\
  -d '{"channel":"webhook","url":"https://example.com/hook","level":"info","title":"Test","message":"Hello"}'
\`\`\`

---

{... 继续覆盖剩余端点 ...}

---

## 中文版

### 目录

- [认证](#认证)
- [错误响应格式](#错误响应格式)
- [端点列表](#端点列表)

### 认证

大多数端点**不需要认证**。下列管理类端点（涉及状态变更或外部副作用）要求 `X-API-Key` HTTP 头：

| 端点 | 需要认证 |
|------|---------|
| `POST /agent/v1/notify/test` | **是** (`X-API-Key`) |

设置环境变量 `AEB_API_KEY` 启用认证。若 `AEB_API_KEY` 未设置，认证中间件等同 no-op（向后兼容现有部署）。

\`\`\`bash
export AEB_API_KEY=$(openssl rand -hex 32)
curl -H "X-API-Key: $AEB_API_KEY" http://localhost:3100/agent/v1/notify/test
\`\`\`

### 错误响应格式

所有错误返回统一 JSON 结构：

\`\`\`json
{
  "success": false,
  "error": "Error",
  "message": "可读错误描述",
  "code": "ERROR_CODE",
  "statusCode": 400
}
\`\`\`

| HTTP 状态 | 常见 Code |
|-----------|----------|
| 400 | `MISSING_FIELD`、`INVALID_VALUE`、`MISSING_URL`、`INVALID_CODE_FORMAT` |
| 401 | `UNAUTHORIZED`（X-API-Key 缺失或错误） |
| 403 | `RATE_LIMITED` |
| 404 | `NOT_FOUND` |
| 503 | `SERVICE_NOT_CONFIGURED`（适配器/服务未初始化） |

### 端点列表（中文摘要）

| # | 方法 | 路径 | 描述 | 认证 |
|---|------|------|------|------|
| 1 | GET | `/agent/v1/health` | 服务健康检查 | 否 |
| 2 | GET | `/agent/v1/balance` | 查询账户余额 | 否 |
| 3 | GET | `/agent/v1/usage/summary` | 用量摘要 | 否 |
| 4 | GET | `/agent/v1/models/capabilities` | 模型能力列表 | 否 |
| 5 | POST | `/agent/v1/recommend` | 模型推荐 | 否 |
| 6 | POST | `/agent/v1/optimize` | 调用前预算审批 | 否 |
| 7 | POST | `/agent/v1/refuel/redeem` | 激活码兑换 | 否 |
| 8 | POST | `/agent/v1/keys/issue` | 发放 API Key | 否 |
| 9 | POST | `/agent/v1/docs/render` | 生成接入文档 | 否 |
| 10 | POST | `/agent/v1/session/report` | 上报会话结果 | 否 |
| 11 | GET | `/agent/v1/session/summary` | 会话摘要 | 否 |
| 12 | GET | `/agent/v1/ops/snapshot` | 运营快照 | 否 |
| 13 | GET | `/agent/v1/ops/report` | 运营趋势报告 | 否 |
| 14 | GET | `/agent/v1/ops/energy` | 能量效率报告 | 否 |
| 15 | POST | `/agent/v1/ops/start` | 启动定时监控 | 否 |
| 16 | POST | `/agent/v1/ops/stop` | 停止定时监控 | 否 |
| 17 | POST | `/agent/v1/meter/record` | 记录 token 消耗 | 否 |
| 18 | GET | `/agent/v1/meter/stats` | 查询 token 统计 | 否 |
| 19 | GET | `/agent/v1/meter/report` | 生成 token 报告 | 否 |
| 20 | GET | `/agent/v1/meter/breakdown` | token 详细分解 | 否 |
| 21 | GET | `/agent/v1/routing/status` | 路由状态 | 否 |
| 22 | GET | `/agent/v1/routing/log` | 路由切换日志 | 否 |
| 23 | POST | `/agent/v1/routing/force` | 强制切换路由 | 否 |
| 24 | GET | `/agent/v1/notify/config` | 通知配置只读 | 否 |
| 25 | POST | `/agent/v1/notify/test` | 发送测试通知 | **是** (`X-API-Key`) |
| 26 | GET | `/agent/v1/refuel/status` | 充值/降级状态 | 否 |

> 详细字段说明见上方 English Version 各端点章节（中英对照）。

---

## License

MIT — See [LICENSE](../LICENSE) for details.
```

**填充规则**（必须严格遵循）：
1. **每个端点至少一个独立 H5 章节**（`##### 方法 /path`），含：描述（中英）/ Auth / Method / Request（如有）/ Response / Errors / Example
2. **示例 URL 一律使用** `http://localhost:3100`、`https://example.com/hook`、`gateway.example.com` 等占位符；禁止真实地址
3. **示例 API Key 一律使用** `$AEB_API_KEY`（shell 变量）；禁止字面值
4. **HTTP 状态码必须明确**：默认成功为 200；明确列出错误码与对应 statusCode
5. **请求字段必须用表格**（Field / Type / Required / Description），不要用纯散文
6. **响应示例必须是合法 JSON**（可被 `JSON.parse` 解析），可有省略号但要在注释里说明
7. **认证标记**：所有端点的 "Auth" 行必须明确写 `Required` 或 `Not required`；目前只有 1 个 endpoint 是 Required（POST /notify/test）
8. **错误码引用源码**：从 handler 文件中的 `error.code = 'X'` 提取，不要编造
9. **顶部"Total endpoints: 26"**，与 router.js ROUTES 长度一致

**不允许的内容**（Warning 5 修复 — heading 层级统一）：
- 端点标题必须统一使用 H5（`##### METHOD /path`），禁止使用 H3（`### METHOD /path`）
- 中文版只写目录索引表 + 引用 English 版（不要重复 26 个端点详情；维护单一信源）
  </action>
  <verify>
    <automated>test -f docs/API.md &amp;&amp; lines=$(wc -l &lt; docs/API.md) &amp;&amp; [ "$lines" -ge 500 ] &amp;&amp; grep -c '^##### ' docs/API.md | awk '{exit ($1 &gt;= 26 ? 0 : 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `docs/API.md` 存在且 ≥ 500 行
    - `grep -c '^##### ' docs/API.md` ≥ 26（即至少 26 个端点 H5 章节）
    - 不包含 H3 端点标题（`grep -c '^### (POST|GET|PUT|DELETE) ' docs/API.md` 必须为 0）
    - 包含字面量 `## English Version` 和 `## 中文版`
    - 包含字面量 `**Total endpoints:** 26`
    - 包含字面量 `##### POST /agent/v1/notify/test`，且其 Auth 行写明 `**Required**` / `X-API-Key`
    - 包含字面量 `### Error Response Format` 和 `### 错误响应格式`
    - 全文 grep 不出现 `your-real-key`、`production.example.org`、真实邮箱（除占位符 `security@your-org.com`）
    - 全文 grep 不出现 `TODO`、`FIXME`、`XXX`（避免漏填）
    - 端点表格（中文版目录索引）包含 26 行，且每行写明"是/否"是否需认证
  </acceptance_criteria>
  <done>
    `docs/API.md` 完整覆盖 26 个端点的双语 API 文档，README 可链接引用。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 仓库公开化 | API 文档内的示例 URL / Key 进入公开仓库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | docs/API.md 示例 | mitigate | 所有示例使用 `localhost`、`example.com`、`$AEB_API_KEY` 占位符（per D-02、D-15）；PLAN-02 的 .gitleaks.toml 已 allowlist docs/*.md |
| T-07-06 | Spoofing | 文档可能误导用户禁用认证 | mitigate | Auth 段明确说明"未设置 AEB_API_KEY 时跳过认证 = 仅适合开发环境"（per D-14 兼容设计） |
</threat_model>

<verification>
```bash
# 1. 文件存在且非空
test -f docs/API.md && [ "$(wc -l < docs/API.md)" -ge 500 ]

# 2. 端点数 ≥ 26，且全部使用 H5（无 H3 端点标题）
[ "$(grep -c '^##### ' docs/API.md)" -ge 26 ]
[ "$(grep -c '^### (POST|GET|PUT|DELETE) ' docs/API.md)" -eq 0 ]

# 3. 中英双语段落都存在
grep -q '^## English Version' docs/API.md && grep -q '^## 中文版' docs/API.md

# 4. POST /notify/test 标注 X-API-Key
grep -A 5 'POST /agent/v1/notify/test' docs/API.md | grep -q 'X-API-Key'

# 5. 无 TODO / FIXME 残留
! grep -E '(TODO|FIXME|XXX)' docs/API.md

# 6. 既有 217 测试无回归
node --test
```
</verification>

<success_criteria>
- `docs/API.md` 中英双语覆盖全部 26 个端点
- POST /notify/test 明确标注 X-API-Key 认证要求
- 所有示例使用占位符，无真实信息泄漏
- 测试 217/217 通过（不涉及 src/ 改动，预期零回归）
- OPEN-02 完成
</success_criteria>

<output>
完成后创建 `.planning/phases/07-open-source-release/07-PLAN-03-SUMMARY.md`。
</output>
