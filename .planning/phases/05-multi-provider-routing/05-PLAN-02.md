---
plan: "05-PLAN-02"
phase: 5
wave: 2
title: "路由 HTTP 端点 + 服务器集成"
depends_on: ["05-PLAN-01"]
requirements: [ROUT-05]
files_modified:
  - src/server/handlers/routing.js
  - src/server/router.js
  - src/server/index.js
autonomous: true
---

# Plan 02: 路由 HTTP 端点 + 服务器集成

## Objective

新增 3 个 HTTP 端点（`GET /agent/v1/routing/status`、`GET /agent/v1/routing/log`、`POST /agent/v1/routing/force`），将 `multiProviderRouter` 注入 `buildContext`，并在 `server.destroy()` 中清理资源。

## must_haves

```yaml
truths:
  - 3 个路由端点遵循 meter.js 的 requireX guard 模式
  - POST /routing/force 使用 validateBody 验证 name 字段（防注入）
  - buildContext 接受并透传 multiProviderRouter 选项
  - server.destroy() 调用 multiProviderRouter.stop() 清理健康检查定时器
  - 503 错误码当 multiProviderRouter 未配置时返回
```

## Tasks

### Task 1: 创建 src/server/handlers/routing.js

<read_first>
- src/server/handlers/meter.js — requireX guard 模式、validateBody 用法、错误格式
- src/utils/validator.js — validateBody 函数签名和 schema 格式
</read_first>

<action>
创建文件 `src/server/handlers/routing.js`：

```js
const { validateBody } = require('../../utils/validator');

const FORCE_SCHEMA = {
  name: { type: 'string', maxLength: 100, required: true },
};

function requireRouter(context) {
  if (!context.multiProviderRouter) {
    const error = new Error('Multi-provider router not available');
    error.statusCode = 503;
    error.code = 'SERVICE_NOT_CONFIGURED';
    throw error;
  }
}

function parseLogLimit(query) {
  const raw = Number(query.limit ?? 100);
  return Number.isFinite(raw) ? Math.max(1, Math.min(1000, Math.floor(raw))) : 100;
}

async function getRoutingStatus(request, response, context) {
  requireRouter(context);
  return {
    success: true,
    report: context.multiProviderRouter.getReport(),
  };
}

async function getRoutingLog(request, response, context) {
  requireRouter(context);
  const query = request.query || {};
  const limit = parseLogLimit(query);
  return {
    success: true,
    limit,
    log: context.multiProviderRouter.getSwitchLog(limit),
  };
}

async function postRoutingForce(request, response, context) {
  requireRouter(context);
  const body = request.body || {};
  const validation = validateBody(body, FORCE_SCHEMA);
  if (!validation.valid) {
    const error = new Error(`Invalid request: ${validation.errors.map((e) => `${e.field}: ${e.message}`).join('; ')}`);
    error.statusCode = 400;
    error.code = 'VALIDATION_ERROR';
    error.details = validation.errors;
    throw error;
  }
  await context.multiProviderRouter.forceProvider(validation.sanitized.name);
  return {
    success: true,
    active: validation.sanitized.name,
  };
}

module.exports = {
  getRoutingStatus,
  getRoutingLog,
  postRoutingForce,
};
```
</action>

<acceptance_criteria>
- `src/server/handlers/routing.js` 文件存在
- 文件包含 `function requireRouter(`
- 文件包含 `async function getRoutingStatus(`
- 文件包含 `async function getRoutingLog(`
- 文件包含 `async function postRoutingForce(`
- 文件包含 `validateBody(body, FORCE_SCHEMA)`
- 文件包含 `error.statusCode = 503` 在 requireRouter 中
- 文件包含 `module.exports = { getRoutingStatus, getRoutingLog, postRoutingForce }`
</acceptance_criteria>

### Task 2: 在 src/server/router.js 注册 3 个新路由

<read_first>
- src/server/router.js — ROUTES 数组结构，现有 import 列表
</read_first>

<action>
在 `src/server/router.js` 中：

1. 在现有 import 列表末尾添加：
```js
const { getRoutingStatus, getRoutingLog, postRoutingForce } = require('./handlers/routing');
```

2. 在 ROUTES 数组末尾（meter 路由之后）添加：
```js
  // Multi-provider routing (ROUT-01~05)
  { method: 'GET',  path: '/agent/v1/routing/status',  handler: getRoutingStatus },
  { method: 'GET',  path: '/agent/v1/routing/log',     handler: getRoutingLog },
  { method: 'POST', path: '/agent/v1/routing/force',   handler: postRoutingForce },
```
</action>

<acceptance_criteria>
- `src/server/router.js` 包含 `require('./handlers/routing')`
- `src/server/router.js` 包含 `'/agent/v1/routing/status'`
- `src/server/router.js` 包含 `'/agent/v1/routing/log'`
- `src/server/router.js` 包含 `'/agent/v1/routing/force'`
- `node -e "const { ROUTES } = require('./src/server/router'); console.log(ROUTES.length)"` 输出 `23`（原 20 条 + 3 条新路由）
</acceptance_criteria>

### Task 3: 更新 src/server/index.js 集成 multiProviderRouter

<read_first>
- src/server/index.js — buildContext 函数、server.destroy() 实现
</read_first>

<action>
在 `src/server/index.js` 中：

1. 在 `buildContext` 函数的 return 对象中，在 `tokenMeter: options.tokenMeter || null,` 之后添加：
```js
    multiProviderRouter: options.multiProviderRouter || null,
```

2. 在 `server.destroy` 函数中，在 `if (context.routeHealthChecker) context.routeHealthChecker.destroy();` 之后添加：
```js
      if (context.multiProviderRouter) context.multiProviderRouter.stop();
```
</action>

<acceptance_criteria>
- `src/server/index.js` 的 `buildContext` 包含 `multiProviderRouter: options.multiProviderRouter || null`
- `src/server/index.js` 的 `server.destroy` 包含 `context.multiProviderRouter) context.multiProviderRouter.stop()`
- `node -e "const { buildContext } = require('./src/server'); const ctx = buildContext({}); console.log('multiProviderRouter' in ctx)"` 输出 `true`
</acceptance_criteria>

## Verification

```bash
node -e "
const { createServer } = require('./src/server');
const s = createServer({});
const ctx = s._aebContext;
console.log('multiProviderRouter in context:', 'multiProviderRouter' in ctx);
s.destroy();
"
```

期望输出 `multiProviderRouter in context: true`。
